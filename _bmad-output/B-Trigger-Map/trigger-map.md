# Trigger Map

The canonical trigger map hub is [00-trigger-map.md](00-trigger-map.md).

This alias exists because the WDS workflow output contract references `trigger-map.md`, while the document generation sequence creates `00-trigger-map.md` as the navigation hub.

