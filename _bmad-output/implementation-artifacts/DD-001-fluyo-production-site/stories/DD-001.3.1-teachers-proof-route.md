---
baseline_commit: 5350c358c6316419343db5930bf1d5feb9478807
---

# DD-001.3.1: Teachers And Proof Route

**Epic:** 3 - Teachers & Proof and Trial & Pricing implementation  
**Status:** done  
**Size:** Large  
**Depends On:** `DD-001.1.3`, `DD-001.2.2`  
**Route:** `/teachers`  
**Source:** DD-001 scope; final UX layout source; page layout schema Teachers & Proof block; TS-001 HP-002, ER-004, EC-005, AX-005  

## Goal

Build the Teachers & Proof page so trust-sensitive visitors can inspect teacher, credential, lesson, and outcome evidence before booking or comparing full pricing.

## Required Sections

1. Global Header from the default layout.
2. Trust Header.
3. Teacher Cards.
4. Credentials Proof.
5. Lesson Proof.
6. Results And Testimonials.
7. Trust CTA Strip.
8. Footer / Contact Strip from the default layout.

## Objects And Components

| Object | Type | Behavior | States |
| --- | --- | --- | --- |
| Trust header | Section header | Frames page as proof/checking page. | localized uk/en |
| Teacher card | Content card | Shows approved teacher profile, audience fit, portrait, credentials. | approved, hidden, review |
| Credential proof item | Proof item | Shows approved certificate/training/method evidence with alt text. | approved, hidden, review |
| Lesson proof review | Proof/media item | Shows approved or illustrative lesson material with privacy-safe metadata. | approved, hidden, review |
| Testimonial/result item | Proof item | Shows concise approved outcomes or testimonials. | approved, hidden |
| Trust CTA strip | CTA section | Routes to `/pricing`, `/programs`, or Telegram with available context. | generic, path-aware |

## Implementation Notes

- Use `app/pages/teachers.vue`.
- Teacher/proof/testimonial rendering must consume content-readiness helpers from `DD-001.1.3`.
- If real teacher profiles are outside public scope, hide individual teacher cards or render neutral process copy only if approved.
- Do not imply measured outcomes, guaranteed improvement, or verified results without approved proof.
- Meaningful media requires accurate alt text; decorative media should be hidden from assistive tech.
- Emit `teacher_proof_view` when proof surfaces are observed or activated.
- Do not duplicate Home path chooser or full pricing table here.

## Tasks/Subtasks

- [x] Add a dependency-free `/teachers` route verification script (`scripts/verify-teachers-route.mjs`, `test:teachers-route`) and confirm it fails against the starter route.
- [x] Implement `/teachers` route composition in approved section order: Trust Header, Teacher Cards, Credentials Proof, Lesson Proof, Results And Testimonials, Trust CTA Strip.
- [x] Render teacher cards, credential proof items, lesson proof, and testimonials from the shared typed content records (`teacherProfiles`, `proofAssets` filtered by `usageContext`, `testimonials`) so non-teachers records stay hidden without layout gaps.
- [x] Add localized `teachersSections` copy to `uk.json` and `en.json` with equivalent structure.
- [x] Keep visual media honest: decorative portrait panels with stable aspect ratio hidden from assistive tech; no meaningful media without approved assets.
- [x] Emit `teacher_proof_view` with route, locale, surface, and trigger context through the tracking adapter on observed and activated proof surfaces.
- [x] Wire Trust CTA Strip to `/pricing`, `/programs`, and Telegram through shared CTA contracts.
- [x] Verify direct-load, keyboard traversal, responsive widths, tracking, production content vocabulary, and production build.

### Review Findings

- [x] [Review][Patch] `TeacherCard`'s required `id` prop was inert — declared in `defineProps` but never used in the template. Fixed following the sibling-card convention: `id` now wires `aria-labelledby` on the article + heading id on the name; also added the missing emptiness guard on the fit block and switched `v-for` keys from translated strings to indexes [app/components/teachers/TeacherCard.vue:15]
- [x] [Review][Patch] `TeacherTestimonial` placed the attribution `<footer>` inside `<blockquote>` (non-conforming per WHATWG). Fixed: restructured as `<figure>` + `<blockquote>` + `<figcaption>` with identical classes and visual layout [app/components/teachers/TeacherTestimonial.vue:9]
- [x] [Review][Patch] Vacuous verifier assertions — bare `/surface/` and `/trigger/` regexes matched type imports alone. Fixed: now assert the payload carries `surface`/`trigger` fields and that observed surfaces report the `observe` trigger [scripts/verify-teachers-route.mjs:210]
- [x] [Review][Patch] `verifyTeachersMessages` never asserted the `learningPaths.*.title` keys used for audience-fit labels. Fixed: assertions added for exam/kids/adult in both locales [scripts/verify-teachers-route.mjs:315]
- [x] [Review][Defer] Trust CTA strip's `teacher_proof_view` activate payloads are identical for the Pricing, Programs, and Telegram buttons (`surface: 'trust-cta'`, `trigger: 'activate'`), so pricing vs programs clicks are indistinguishable in analytics — deferred, spec-conformant (surface-level granularity per DD-001.3.3); enriching the payload with a CTA-target field is a product analytics decision [app/components/sections/TeachersCtaStripSection.vue:21]
- [x] [Review][Defer] Proof-column kind/usage mapping is not pinned by verification: a content edit moving an asset off `kind: 'classroom'`/`usageContext: 'teachers'` leaves a column rendering its title, description, and privacy note around zero assets while `test:teachers-route` passes — deferred, joins the deferred-work entry on deriving verify-script checks from actual data [app/components/sections/TeachersProofColumnsSection.vue:24]
- [x] [Review][Defer] Agent-Verifiable readiness-gating AC has no implementation (readiness system removed at `51c39ce`) — deferred with the same resolution as DD-001.3.2: the site carries only real approved content with no preview/draft states by policy; production content changes happen through an explicit task or manual edit
- [x] [Review][Defer] Verify script asserts source text/formatting, not behavior (exact observer constants of a file outside its scope, whitespace-sensitive `defineProps` regexes, duplicated `data-proof-surface` checks) — deferred, already logged in deferred-work.md (grep-source verifier convention shared by all route verify scripts)

## Acceptance Criteria

### Agent-Verifiable

- [ ] `/teachers` loads directly.
- [ ] Trust Header, Teacher Cards, Credentials Proof, Lesson Proof, Results/Testimonials, Trust CTA Strip, and footer render in approved order.
- [ ] Teacher/proof/testimonial records with `production`, `production_required`, or `production_reviewed` do not render as approved public proof in production build.
- [ ] Hidden proof records do not leave broken empty layout gaps.
- [ ] Rendered media has stable dimensions or aspect ratios.
- [ ] Rendered meaningful media has accurate alt text.
- [ ] `teacher_proof_view` emits through the tracking adapter.
- [ ] CTA strip links to `/pricing`, `/programs`, and Telegram through shared contracts.
- [ ] `npm run build` passes.

### User-Evaluable

- [ ] The page increases trust without feeling like a unavailable proof wall.
- [ ] The absence of final approved assets is handled honestly and cleanly.
- [ ] A parent can understand teacher fit, credentials, and lesson visibility before booking.

## Verification

- Run `npm run build`.
- Direct-load `/teachers`.
- Test production build behavior with production teacher/testimonial records.
- Tab through teacher cards, proof links, CTA strip, header, and footer.
- Check 375, 428, 768, 1440, and 1920 widths for no overlap.

## Out Of Scope

- Full pricing details.
- Final production teacher/profile/proof content supply.
- Analytics vendor installation.

## Dev Agent Record

### Implementation Plan

- Added a dependency-free `/teachers` route verifier first (`scripts/verify-teachers-route.mjs`) and confirmed it failed against the starter route shell.
- Implemented the Teachers & Proof route in `app/pages/teachers.vue` using shared `UiSection` and `UiButtonLink` primitives, following the approved layout schema: Trust Header, three-column proof area (Teacher Cards | Credentials Proof | Lesson Proof), Results And Testimonials, Trust CTA Strip.
- Rendered all proof content from the shared typed records: `teacherProfiles`, `proofAssets` filtered by `usageContext.includes('teachers')`, and `testimonials`. The pricing-only `lesson-format-board` asset is excluded by the data filter, so hidden records leave no layout gaps.
- Extended the shared tracking contract with `TeacherProofSurface` and an optional `TrackingPayload.surface` field so `teacher_proof_view` events distinguish which proof surface was observed or activated (mirrors the `trigger` extension added in DD-001.2.2).
- Added localized `teachersSections` copy to both locale files, reusing existing `teacherProfiles`, `proofAssets`, `testimonials`, and `learningPaths` message records.
- Portraits render as decorative fixed-aspect initials panels (`aria-hidden="true"`) because no approved portrait assets exist; no `<img>` elements render on the page, so no meaningful media ships without approved assets.

### Debug Log

- `npm run test:teachers-route` initially failed as expected against the starter route (red phase), then failed on a `telegram_click` literal assertion; the page delegates through the shared `trackTelegramClick` contract, so the assertion was aligned with the programs-route verifier.
- Runtime browser audit (Chromium): direct `/teachers` load, section order top-to-bottom, 4 `teacher_proof_view` observe events (one per proof surface), activate event on CTA click, navigation to `/pricing`, English copy after `?lang=en` hydration, keyboard traversal reaching all CTA strip links, and 375/428/768/1440/1920 widths with zero horizontal overflow — all passed.
- Known pre-existing site-wide quirk (verified identical on `/programs`): direct-loading with `?lang=en` renders English copy after hydration, but internal link `href`s and the Telegram context keep default-locale values until the header language control is used; interactive switching updates everything correctly. Recorded in `deferred-work.md`; belongs to the shared `useLocale`/hydration layer, not this route.
- AC note: the criterion referencing records with `production`/`production_required`/`production_reviewed` statuses describes the removed readiness-status system (deleted in baseline `51c39ce`, wording carried over by the production-content rename). No status metadata exists in content records anymore; the enforceable equivalent — every rendered teacher/proof/testimonial record is production content and the vocabulary verification passes — is satisfied via `npm run test:production-content`.
- No sprint status YAML exists under implementation artifacts; progress is tracked in this story file only.

### Completion Notes

- `/teachers` renders Trust Header (with trust note), Teacher Cards, Credentials Proof, Lesson Proof, Results And Testimonials, and Trust CTA Strip in the approved order between the global header and footer.
- Teacher cards show localized name, role, bio, audience fit (shared `learningPaths` titles), and credential chips; the credentials column ties every credential to a named teacher and carries the method-evidence proof item plus proof note; the lesson proof column carries the classroom proof item plus privacy note.
- `teacher_proof_view` emits through the tracking adapter with route, locale, sourceRoute, surface, and observe/activate trigger; surfaces are deduplicated per visit via IntersectionObserver with the same visibility guard as the programs route.
- Trust CTA Strip routes to `/pricing`, `/programs`, and Telegram (`ask_teacher` intent, teachers source route) through shared CTA contracts with locale-preserving internal links.
- All verification scripts pass (`test:teachers-route`, `test:production-content`, and the six pre-existing suites) and `npm run build` completes.

### File List

- app/pages/teachers.vue
- app/data/content/types.ts
- app/composables/useTracking.ts
- app/i18n/locales/en.json
- app/i18n/locales/uk.json
- package.json
- scripts/verify-teachers-route.mjs
- scripts/verify-production-content.mjs
- _bmad-output/project-context.md
- _bmad-output/implementation-artifacts/deferred-work.md
- _bmad-output/implementation-artifacts/DD-001-fluyo-production-site/stories/DD-001.3.1-teachers-proof-route.md

### Change Log

- 2026-07-14: Implemented DD-001.3.1 Teachers And Proof route: route composition in approved section order, shared-content proof rendering with usage-context filtering, `teacher_proof_view` tracking with surface/trigger context (shared `TrackingPayload.surface` extension), localized `teachersSections` copy, trust CTA strip wiring, route verification script, and runtime browser audit. Marked story ready for review.
- 2026-07-14: User feedback applied — public copy tone rework. Rewrote specimen-style wording («Приклад формату фідбеку…», "Exam progress example") and site-describing copy («Сторінка пояснює…», "This page explains…", "Each card shows…") across both locales into warm, direct visitor-facing production copy (teachers intro/SEO, `teachersSections`, `proofAssets`, asset alt texts, home trial-pricing description). Added locale-scoped blocked patterns (приклад/зразок/example/this-page-explains and similar) to `scripts/verify-production-content.mjs` and recorded the tone + no-specimen-wording rules in `_bmad-output/project-context.md` so the vocabulary check enforces this going forward.
- 2026-07-14: Second feedback pass — removed the «фрагмент»/"snapshot" specimen family from public copy («Фрагмент дитячого заняття» → «Як проходить дитяче заняття», "Kids lesson snapshot" → "Inside a kids lesson", kids proof cue and alt texts rewritten, home "Proof snapshot" eyebrow → "Trust at a glance") and added `фрагмент`/`snapshot` to the locale-scoped blocked patterns in `scripts/verify-production-content.mjs`.
