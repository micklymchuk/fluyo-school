---
baseline_commit: 51c39cea3fa097ec75942bba9efcb9e7e9aa814f
---

# DD-001.2.2: Learning Paths Route

**Epic:** 2 - Home and Learning Paths implementation  
**Status:** done  
**Size:** Large  
**Depends On:** `DD-001.2.1`  
**Route:** `/programs`  
**Source:** DD-001 scope; final UX layout source; page layout schema Learning Paths block; TS-001 HP-001 through HP-003 and EC-001  

## Goal

Build the Learning Paths page that gathers Exam Preparation, Kids & Parents, and Adults Speaking into one comparison route with path-aware CTAs.

## Required Sections

1. Global Header from the default layout.
2. Learning Paths Header.
3. Exam Preparation.
4. Kids & Parents.
5. Adults Speaking.
6. Path Price Hints.
7. Learning Paths CTA Strip.
8. Footer / Contact Strip from the default layout.

## Objects And Components

| Object | Type | Behavior | States |
| --- | --- | --- | --- |
| Learning Paths header | Section header | Orients visitors to compare path fit. | localized uk/en |
| Exam section | Program panel | Shows exam fit, seriousness, structure, diagnostic-trial cue. | default, anchor target |
| Kids section | Program panel | Shows parent reassurance, child engagement, progress visibility cue. | default, anchor target |
| Adults section | Program panel | Shows adult recognition, real-life speaking situations, supportive method cue. | default, anchor target |
| Path price hint | Price hint | Shows concise path-specific price signal when gate allows. | approved, hidden, review |
| CTA strip | Section CTA | Routes to `/pricing?path=exam|kids|adult`, `/teachers`, or Telegram with context. | selected path, generic |

## Implementation Notes

- Use `app/pages/programs.vue`.
- Anchor ids must be stable: `exam-preparation`, `kids-parents`, `adults-speaking`.
- Direct deep links must land with readable header offset.
- Program sections should use shared section/card primitives from Epic 0.
- Do not create separate exam, kids, or adult routes.
- Do not include full pricing table, full teacher gallery, full testimonial wall, or long FAQ.

## Tasks/Subtasks

- [x] Implement `/programs` route composition in approved section order.
- [x] Render Exam Preparation, Kids & Parents, and Adults Speaking sections with stable anchors and readable hash offsets.
- [x] Wire path-aware CTAs to `/pricing?path=exam|kids|adult` and Telegram context through shared contracts.
- [x] Emit `program_path_view` with route, locale, and path context on observed or activated paths.
- [x] Render path price hints from production-facing content without replacing the Trial & Pricing page.
- [x] Verify direct-load, anchor navigation, Home path-card navigation, English locale order, responsive widths, tracking, and production build.

### Review Findings

- [x] [Review][Decision→Fixed] Internal CTA links drop the `?lang` query, resetting English users to Ukrainian — resolved site-wide: added `withLocaleQuery`/`useLocalizedTo` to `app/composables/useLocale.ts` and applied it in `UiButtonLink`, `HeaderLink`, `UiLogo`, `FooterContactStrip`, and `HomePathCardsSection`, covering every internal link renderer.
- [x] [Review][Decision→Fixed] `program_path_view` semantics — added `trigger: 'observe' | 'activate'` to the shared `TrackingPayload` contract and all emissions (clicks intentionally re-emit as activations, now distinguishable), and removed the no-`IntersectionObserver` fallback that fabricated views on mount.
- [x] [Review][Decision→Fixed] Stale AC — amended "respect content-readiness gates" to "render production-facing price content shared with the Trial & Pricing page" per the baseline gate removal; deviation recorded in the Change Log.
- [x] [Review][Decision→Resolved] Tooling changes bundled into the story diff (`.gitignore`, `_bmad/_config/manifest.yaml`, `CLAUDE.md`) — to be committed separately from the story; noted in the Change Log.
- [x] [Review][Decision→Fixed] Duplicated path copy — panel and price-hint titles now render from the shared `learningPaths.*.title` keys; the duplicate `programsSections.paths.*.title` keys were removed from both locales.
- [x] [Review][Decision→Dismissed] Program panels keep their custom two-column shell — the layout genuinely differs from `UiSection`'s header shape and the visuals were already runtime-audited; accepted deliberately.
- [x] [Review][Patch] Removed dead `pathOrder` array from both locale files and the verify script; render order is asserted through `learning-paths.ts` data order.
- [x] [Review][Patch] Moved the path Telegram format mapping into shared data as `LearningPath.telegramFormat` [app/data/content/learning-paths.ts]
- [x] [Review][Patch] Verify script now checks `pages.programs.intro.*` and `learningPaths.*.title` in both locales [scripts/verify-programs-route.mjs]
- [x] [Review][Patch] `IntersectionObserver` now uses a graded threshold ladder plus a ratio-or-half-viewport visibility guard so tall panels on short viewports still emit [app/pages/programs.vue]
- [x] [Review][Patch] `scroll-margin-top: 9rem` replaced with the token-backed `scroll-mt-36` utility [app/pages/programs.vue]
- [x] [Review][Patch] Restored the trailing hard-break on the Status metadata line.
- [x] [Review][Patch] `readJson` now fails with a labeled error naming the offending file [scripts/verify-programs-route.mjs]
- [x] [Review][Patch] Removed the `handleGenericTelegramClick` pass-through; `trackTelegramClick` is bound directly [app/pages/programs.vue]
- [x] [Review][Defer] `/pricing?path=...` context has no consumer — pricing page is still the starter stub, so the param dies on arrival [app/pages/pricing.vue] — deferred, consumed by the upcoming pricing story (DD-001.3.x)

## Acceptance Criteria

### Agent-Verifiable

- [ ] `/programs` loads directly.
- [ ] `/programs#exam-preparation`, `/programs#kids-parents`, and `/programs#adults-speaking` reach readable section positions.
- [ ] Exam, Kids & Parents, and Adults Speaking sections render in the approved order.
- [ ] Each path section exposes a CTA to `/pricing?path=<path>` or Telegram with equivalent path context.
- [ ] Learning Paths CTA Strip can route to `/pricing`, `/teachers`, or Telegram.
- [ ] `program_path_view` emits with route, locale, and path context when a path is observed or activated.
- [ ] Path price hints render production-facing price content shared with the Trial & Pricing page.
- [ ] English locale preserves the same section order and anchors.
- [ ] `npm run build` passes.

### User-Evaluable

- [ ] Visitors can compare the three path choices without route sprawl.
- [ ] Danylo sees exam seriousness, Olena sees parent reassurance, and Marta sees adult speaking recognition.
- [ ] Price hints clarify direction without replacing the Trial & Pricing page.

## Verification

- Run `npm run build`.
- Direct-load each anchor.
- Navigate from Home path cards into each anchor.
- Navigate from each path CTA into `/pricing?path=exam`, `/pricing?path=kids`, and `/pricing?path=adult`.
- Check 375, 428, 768, 1440, and 1920 widths for no horizontal overflow.

## Out Of Scope

- Full teacher profile cards.
- Full pricing table.
- Final production content replacement.

## Dev Agent Record

### Implementation Plan

- Added a dependency-free `/programs` route verifier first and confirmed it failed against the starter route.
- Implemented the Learning Paths route in `app/pages/programs.vue` using shared `UiSection` and `UiButtonLink` primitives.
- Added direct Nuxt i18n `t()` content for the route under `programsSections` in both locale files.
- Reused shared learning-path, pricing, Telegram CTA, and tracking contracts instead of adding route-specific data handlers.

### Debug Log

- `npm run test:programs-route` initially failed because `/programs` did not use Nuxt i18n and had no story sections.
- `npm run build` initially failed on non-existent Tailwind utilities; replaced them with existing project tokens.
- Runtime browser audit initially showed hash targets landing too close to the sticky header; increased `scroll-margin-top` for program panels.
- Runtime browser audit passed for direct `/programs`, all three hash anchors, Home path-card navigation, `program_path_view`, English order, and 375/428/768/1440/1920 widths.
- No sprint status YAML exists under implementation artifacts; progress is tracked in this story file only.

### Completion Notes

- `/programs` now renders the Learning Paths header, Exam, Kids & Parents, Adults Speaking, Path Price Hints, and CTA Strip in the required order.
- Program panels expose stable `exam-preparation`, `kids-parents`, and `adults-speaking` anchors with readable direct-link offsets.
- Path CTAs preserve context through `/pricing?path=...` links and Telegram URLs built through the shared CTA resolver.
- `program_path_view` emits with route, locale, and path context when program sections are observed or activated.
- Price hints reuse shared production-facing price items and keep the full comparison on `/pricing`.

### File List

- app/pages/programs.vue
- app/data/content/pages.ts
- app/data/content/types.ts
- app/data/content/learning-paths.ts
- app/composables/useLocale.ts
- app/composables/useTracking.ts
- app/components/ui/UiButtonLink.vue
- app/components/ui/UiLogo.vue
- app/components/navigation/HeaderLink.vue
- app/components/navigation/FooterContactStrip.vue
- app/components/sections/HomePathCardsSection.vue
- app/i18n/locales/en.json
- app/i18n/locales/uk.json
- package.json
- scripts/verify-programs-route.mjs
- _bmad-output/implementation-artifacts/DD-001-fluyo-production-site/stories/DD-001.2.2-learning-paths-route.md

### Change Log

- 2026-07-13: Implemented DD-001.2.2 Learning Paths route, added route verification, validated anchors/tracking/responsive behavior, and marked story ready for review.
- 2026-07-14: Code review applied. Amended the stale content-readiness-gate AC to the production-content mandate (gate infrastructure was removed in baseline `51c39ce`). Added site-wide `?lang` preservation for internal links via `useLocalizedTo`. Added `trigger: 'observe' | 'activate'` to `program_path_view`, removed the no-IntersectionObserver fallback, and added a visibility guard for tall panels. Moved the path Telegram format mapping into shared `learning-paths.ts` data. Consolidated panel titles onto shared `learningPaths.*.title` keys and removed the dead `pathOrder` locale key. Tooling files (`.gitignore`, `_bmad/_config/manifest.yaml`, `CLAUDE.md`) are intentionally not part of this story and should be committed separately.
