---
baseline_commit: 5350c358c6316419343db5930bf1d5feb9478807
---

# DD-001.3.2: Trial And Pricing Route

**Epic:** 3 - Teachers & Proof and Trial & Pricing implementation  
**Status:** done  
**Size:** Large  
**Depends On:** `DD-001.1.2`, `DD-001.1.3`, `DD-001.2.2`  
**Route:** `/pricing`  
**Source:** DD-001 scope; final UX layout source; page layout schema Trial & Pricing block; TS-001 HP-001 through HP-003, ER-001, ER-002, EC-002, EC-003  

## Goal

Build the Trial & Pricing page that clarifies the paid first step, lesson formats, included value, practical FAQ, and Telegram booking with path and format context.

## Required Sections

1. Global Header from the default layout.
2. Trial First Step.
3. Individual Format.
4. Pair Format.
5. Mini-Group Format.
6. What Is Included.
7. Practical FAQ.
8. Final Booking CTA.
9. Footer / Contact Strip from the default layout.

## Objects And Components

| Object | Type | Behavior | States |
| --- | --- | --- | --- |
| Trial first step | Pricing hero | Shows paid trial price/duration and included value when approved. | generic, exam, kids, adult, hidden/blocked |
| Format block | Pricing block | Shows individual, pair, and mini-group format fit and terms. | approved, hidden, review |
| Included item | Info item | Shows teacher matching, live lesson, materials, feedback/progress. | approved, hidden |
| FAQ row | Accordion | Answers practical payment, schedule, online format, language, rescheduling questions. | collapsed, expanded, hidden |
| Final booking CTA | CTA section | Opens Telegram with path/format/source route/locale context or generic fallback. | path-aware, format-aware, generic |

## Implementation Notes

- Use `app/pages/pricing.vue`.
- Known query contexts: `/pricing?path=exam`, `/pricing?path=kids`, `/pricing?path=adult`.
- Unknown query context falls back to generic pricing content without a visible error.
- Pricing content is launch-sensitive; public rendering must obey the production-content validation.
- FAQ answers about payment, scheduling, rescheduling, online format, and trial terms must be approved or hidden.
- Emit `pricing_view`, `telegram_click`, and `telegram_context` through the tracking adapter.

## Tasks/Subtasks

- [x] Add a dependency-free `/pricing` route verification script (`scripts/verify-pricing-route.mjs`, `test:pricing-route`) and confirm it fails against the starter route.
- [x] Extend shared typed content contracts: `pricingFormats` (individual, pair, mini-group with Telegram format, linked price items, and path fit), `pricingIncludedItems`, a pair-lesson price item, and practical FAQ records for payment, rescheduling, and online format.
- [x] Implement `/pricing` route composition in approved section order: Trial First Step, Individual Format, Pair Format, Mini-Group Format, What Is Included, Practical FAQ, Final Booking CTA.
- [x] Adapt Trial First Step helper and final CTA description to `?path=exam|kids|adult` context; unknown or missing path falls back to generic paid-trial content without a visible error.
- [x] Add localized `pricingSections` copy plus new `priceItems`/`faqItems` entries to `uk.json` and `en.json` with equivalent structure.
- [x] Render the Practical FAQ through the keyboard-accessible `UiAccordion` from shared `faqItems` records.
- [x] Emit `pricing_view`, `telegram_context`, and `telegram_click` with available path/format/route/locale context through the tracking adapter; rapid CTA clicks must keep URLs and payloads valid.
- [x] Verify direct-load of `/pricing` and all path variants, FAQ keyboard access, responsive widths, tracking, production content vocabulary, and production build.

### Review Findings

- [x] [Review][Patch] Home page gained a fifth, orphaned price card — the new shared `pair-lessons` price item also rendered on `/` because `HomeTrialPricingSection.vue` looped the entire `priceItems` array. Resolved per review decision: the home summary is pinned to an explicit `summaryPriceItemIds` list (`trial`, `exam-group`, `kids-group`, `adult-individual`), so pricing-scoped data additions no longer change the home page [app/components/sections/HomeTrialPricingSection.vue:75]
- [x] [Review][Defer] Agent-Verifiable AC "non-approved price, trial, or policy FAQ records do not render as approved public truth in production build" has no implementation — no readiness/approval gating exists in the content records or sections (readiness system was removed at baseline `51c39ce`); the Dev Agent Record documents `npm run test:production-content` as the enforceable equivalent and the AC checkbox stays unchecked — deferred: the site carries only real approved content with no preview/draft states by policy; production content changes happen through an explicit task or manual edit, so record-level gating is not needed now
- [x] [Review][Patch] Dead guard in `pricing_view` watcher — `watch` on a primitive computed only fires when the value changes, so `nextPath !== previousPath` was always true; condition removed [app/pages/pricing.vue:31]
- [x] [Review][Defer] `verify-pricing-route.mjs` hardcodes id lists (`pricingFormatIds`, `priceItemIds`, `faqItemIds`) duplicated from the data files and checks only those known ids, so a new data record added without i18n entries passes silently [scripts/verify-pricing-route.mjs:24] — deferred, shared convention with the programs/teachers verify scripts; improve all three together
- [x] [Review][Defer] `verify-pricing-route.mjs` regexes assert exact source formatting (prop order/whitespace in `defineProps`, literal template attribute matches), so harmless reformatting breaks verification without any behavior change [scripts/verify-pricing-route.mjs:200] — deferred, inherent to the grep-source verifier approach used by all route verify scripts

### Review Findings (shared-files chunk)

- [x] [Review][Patch] The «демо» blocked pattern was dead code — JS `\b` is ASCII-only and never matches around Cyrillic letters, so `/\bдемо\b/iu` could not fire on any Ukrainian text. Fixed with Unicode lookarounds; also bounded «приклад» so «наприклад»/«прикладна» stay legal, and normalized Windows path separators in locale-file detection [scripts/verify-production-content.mjs:59]
- [x] [Review][Patch] en `homeSections.proofSnapshot.description` was page-describing meta-language ("The first scan shows…"); rewritten to mirror the warm Ukrainian primary voice [app/i18n/locales/en.json]
- [x] [Review][Patch] `faqItems.english-switch.answer` shipped developer prose to visitors in both locales (literal backticks around `lang=en`, "route structure"/«структури маршруту»); rewritten as warm instructions about the EN menu control [app/i18n/locales/uk.json, en.json]
- [x] [Review][Patch] uk `teachersSections.teacherCards.title` was an English calque («…людьми за уроками»); now «Познайомтесь із людьми, які ведуть уроки» [app/i18n/locales/uk.json]
- [x] [Review][Patch] `teachersSections.ctaStrip.title` used an ASCII hyphen as a dash in both locales where sibling strings use «—»; fixed [app/i18n/locales/uk.json, en.json]
- [x] [Review][Defer] The vocabulary check is a tripwire, not a parser: declined-form page-describing copy («На цій сторінці…», "This page lists…") evades the enumerated patterns, and the `\bsnapshot\b(?!":)` exemption is coupled to exact JSON formatting — deferred, logged in deferred-work.md

## Acceptance Criteria

### Agent-Verifiable

- [ ] `/pricing` loads directly.
- [ ] `/pricing?path=exam`, `/pricing?path=kids`, and `/pricing?path=adult` adapt heading/helper or CTA state to known context.
- [ ] `/pricing?path=unknown` falls back to generic pricing content without broken state.
- [ ] Trial First Step, three format blocks, included items, FAQ, final CTA, and footer render in approved order.
- [ ] Telegram CTA opens through shared helper with available path/format context.
- [ ] Missing path or format context uses a generic paid-trial fallback.
- [ ] Rapid CTA clicks do not create invalid URLs or malformed tracking payloads.
- [ ] `pricing_view`, `telegram_click`, and `telegram_context` emit with available context.
- [ ] production/outside public scope price, trial, or policy FAQ records do not render as approved public truth in production build.
- [ ] `npm run build` passes.

### User-Evaluable

- [ ] The paid trial and format options are understandable before Telegram.
- [ ] Context-aware pricing feels helpful without hiding generic booking.
- [ ] FAQ answers feel practical and trustworthy, not speculative.

## Verification

- Run `npm run build`.
- Direct-load `/pricing`, `/pricing?path=exam`, `/pricing?path=kids`, `/pricing?path=adult`, and `/pricing?path=unknown`.
- Test FAQ keyboard access and expanded/collapsed state.
- Click the final Telegram CTA from generic and path-specific states.
- Check mobile widths for long Ukrainian/English pricing labels and FAQ answers.

## Out Of Scope

- Supplying final price values.
- Payment, scheduling, or CRM integrations.
- Analytics vendor selection.

## Dev Agent Record

### Implementation Plan

- Added a dependency-free `/pricing` route verifier first (`scripts/verify-pricing-route.mjs`, `test:pricing-route`) and confirmed it failed against the starter route shell (red phase).
- Extended the shared typed content contracts: `PricingFormatId`/`PricingFormat` (Telegram format context, linked price items, path fit) and `PricingIncludedItemId`/`PricingIncludedItem` in `types.ts`; `pricingFormats` and `pricingIncludedItems` records in `pricing.ts`; a `pair-lessons` price item (`PriceFormat` gained `pair`); and three practical FAQ records (`payment-method`, `reschedule-policy`, `online-format`) in `faq.ts`, ordered payment → schedule → rescheduling → online format → language.
- Implemented the route in `app/pages/pricing.vue` with shared `UiSection`, `UiButtonLink`, and `UiAccordion` primitives in the approved order: Trial First Step (pricing hero with trial price/duration, included value, path note, booking CTA), the three format blocks rendered from `pricingFormats` (individual → pair → mini-group) with linked `priceItems`, What Is Included from `pricingIncludedItems`, Practical FAQ from `faqItems`, and the inverse Final Booking CTA.
- Path context: `?path` is validated against shared `learningPaths` ids; a known path adapts the trial path note, marks fitting format blocks with a "recommended" badge, sets the final CTA description, and threads path + recommended Telegram format into every Telegram CTA. Unknown or missing path resolves to `generic` with no visible error.
- Telegram CTAs resolve through the shared `useTelegramCta`/`resolveTelegramCta` contract: trial hero (`book_trial`, sourceRoute `pricing`), per-format CTAs (`ask_price`, format-aware), final CTA (`book_trial`, sourceRoute `final-booking`). `pricing_view` emits on mount and on path-context change; `telegram_context` + `telegram_click` emit on every CTA click.

### Debug Log

- `npm run test:pricing-route` failed as expected against the starter route (`pricing page must use Nuxt i18n directly`), then passed after implementation — red/green confirmed.
- Runtime browser audit (Chromium/Playwright, 39 checks, all passed): direct-load section order including footer; `pricing_view` payloads for generic/exam/kids/adult/unknown (unknown → generic, no error, no recommended badge); distinct path notes per known path; Telegram URLs carrying path/format/locale context for hero, pair-format, and generic states; 5 rapid final-CTA clicks emitting 5 paired `telegram_context`/`telegram_click` events with stable valid URLs; FAQ keyboard traversal (focus, Enter expand/collapse, `aria-expanded`, visible panel, 9 questions); English copy after `?lang=en`; zero horizontal overflow at 375/428/768/1440/1920 widths.
- AC note (same situation as DD-001.3.1): the criterion referencing `production`/outside-public-scope price, trial, or policy FAQ records describes the removed readiness-status system (deleted at baseline `51c39ce`). No status metadata exists in content records; the enforceable equivalent — every rendered price/trial/FAQ record is production content and the vocabulary check passes — is satisfied via `npm run test:production-content`.
- No sprint status YAML exists under implementation artifacts; progress is tracked in this story file only.

### Completion Notes

- `/pricing` renders Trial First Step, Individual/Pair/Mini-Group format blocks, What Is Included, Practical FAQ, and the Final Booking CTA in approved order between the global header and footer.
- Known `?path=exam|kids|adult` contexts adapt the trial path note, format recommendation badges, final CTA description, and all Telegram contexts; unknown/missing path falls back to generic paid-trial content silently.
- The FAQ covers booking, format choice, teacher matching, price finality, payment, schedule, rescheduling, online format, and English interface — all rendered through the keyboard-accessible `UiAccordion`.
- `pricing_view`, `telegram_context`, and `telegram_click` emit through the tracking adapter with route, locale, path, format, and sourceRoute context; rapid clicks stay valid.
- All ten verification scripts pass (`test:pricing-route` plus the nine pre-existing suites) and `npm run build` completes.

### File List

- app/pages/pricing.vue
- app/components/sections/PricingTrialSection.vue
- app/components/sections/PricingFormatsSection.vue
- app/components/pricing/PricingFormatCard.vue
- app/components/pricing/PricingFaqItem.vue
- app/components/sections/PricingIncludedSection.vue
- app/components/sections/PricingFaqSection.vue
- app/components/sections/PricingFinalCtaSection.vue
- app/composables/usePricingPathContext.ts
- app/data/content/types.ts
- app/data/content/pricing.ts
- app/data/content/faq.ts
- app/i18n/locales/uk.json
- app/i18n/locales/en.json
- package.json
- scripts/verify-pricing-route.mjs
- _bmad-output/implementation-artifacts/DD-001-fluyo-production-site/stories/DD-001.3.2-trial-pricing-route.md

### Change Log

- 2026-07-14: Implemented DD-001.3.2 Trial And Pricing route: route composition in approved section order, shared `pricingFormats`/`pricingIncludedItems`/pair price item/practical FAQ content contracts, path-aware trial hero and final booking CTA with generic fallback, keyboard-accessible FAQ accordion, `pricing_view`/`telegram_context`/`telegram_click` tracking, localized `pricingSections` copy, route verification script, and runtime browser audit. Marked story ready for review.
- 2026-07-14: User feedback applied — component structure rework. Split the monolithic `pricing.vue` into dedicated section components following the home page pattern: `PricingTrialSection`, `PricingFormatsSection`, `PricingIncludedSection`, `PricingFaqSection`, `PricingFinalCtaSection` under `app/components/sections/`, with shared `?path` query state extracted into `app/composables/usePricingPathContext.ts`. The page is now a thin composer that keeps only SEO registration and route-level `pricing_view` tracking. `scripts/verify-pricing-route.mjs` updated to enforce the composed structure (page must not inline `UiSection`; per-section contracts checked individually). All verification suites, `npm run build`, and the full 39-check runtime browser audit pass unchanged.
- 2026-07-14: Second feedback pass — extracted the format block into a configurable `app/components/pricing/PricingFormatCard.vue` driven by a typed `format: PricingFormat` prop (prices, recommendation badge, fit/terms copy, and format-aware Telegram CTA all derive from the prop); `PricingFormatsSection.vue` is now a thin loop over `pricingFormats`. Verifier updated to require the card composition and its prop contract. All suites, build, and the 39-check runtime audit pass unchanged.
- 2026-07-14: Third feedback pass — extracted the FAQ row into a presentational `app/components/pricing/PricingFaqItem.vue` configured entirely through typed `id`, `question`, and `answer` string props (no i18n inside the item); `PricingFaqSection.vue` resolves the localized copy and passes it in. Verifier updated to require the item composition and its exact prop contract. All suites, build, and the 39-check runtime audit pass unchanged.
- 2026-07-14: Fourth feedback pass — made `PricingFormatCard.vue` fully independent and presentational: it is configured entirely through typed props (resolved title/labels/fit/terms/CTA strings, a resolved prices list, `recommended` flag, and `ctaHref`) and emits `cta-click` instead of tracking. All business logic (i18n resolution, `priceItems` lookup, path context, `resolveTelegramCta`, `telegram_context`/`telegram_click` tracking) moved up into `PricingFormatsSection.vue`. Verifier updated to enforce the presentational contract (card must not import i18n, content records, Telegram, tracking, or path-context composables). All suites, build, and the 39-check runtime audit pass unchanged.
