---
baseline_commit: c0cd5127468b9283265a12c2d2d915e3157f507a
---

# DD-001.1.2: Telegram CTA And Tracking Contracts

**Epic:** 1 - Content model, locale, routing, and CTA contracts  
**Status:** done
**Size:** Medium  
**Depends On:** `DD-001.1.1`  
**Source:** Architecture AD-5 and AD-9; DD-001 measurement events; TS-001 analytics tests  

## Goal

Centralize Telegram link creation and analytics event emission so all CTA surfaces preserve context consistently.

## Build Scope

- Add `app/composables/useTelegramCta.ts`.
- Add `app/composables/useTracking.ts`.
- Add shared CTA context and tracking payload types if not already defined in `DD-001.1.1`.
- Integrate the CTA helper into the header CTA shell from `DD-001.0.2`.

## Telegram Requirements

- Base URL is `https://t.me/fluyo_manager`.
- Known path contexts: `exam`, `kids`, `adult`, and generic.
- Known format contexts: individual, pair, mini-group, and generic.
- Helper accepts `path`, `format`, `sourceRoute`, `locale`, and `messageIntent`.
- Prepared messages may be used only when approved or safe for review.
- Missing context falls back to a generic paid-trial Telegram link.
- URL encoding is handled in one place.
- Rapid repeated clicks must not corrupt link state or tracking payloads.

## Tracking Requirements

Only these event names may be emitted by production components:

- `path_card_click`
- `program_path_view`
- `pricing_summary_view`
- `pricing_view`
- `teacher_proof_view`
- `telegram_click`
- `telegram_context`

Payloads may include:

- `route`
- `locale`
- `path`
- `format`
- `sourceRoute`
- `messageIntent`

## Implementation Notes

- Use a no-op adapter or console/dev adapter until a concrete analytics vendor is selected.
- Components should call `trackEvent` or an equivalent wrapper rather than vendor APIs.
- Telegram CTA helper should return both the URL and tracking context so CTA components remain thin.

## Acceptance Criteria

### Agent-Verifiable

- [ ] All Telegram URLs are produced by `useTelegramCta`.
- [ ] Header, section, pricing, and final booking CTA surfaces can share one CTA component/helper contract.
- [ ] Unknown or missing context returns a valid generic Telegram URL.
- [ ] Known path contexts produce distinguishable CTA contexts for exam, kids, and adult.
- [ ] Tracking adapter accepts only the seven DD-001 event names.
- [ ] Tracking payloads safely omit or null missing context without throwing.
- [ ] `npm run build` passes.

### User-Evaluable

- [ ] CTA behavior is predictable: every Telegram entry point is clearly a paid-trial contact action.
- [ ] Context preservation helps the manager understand the visitor intent without making the message feel overcomplicated.

## Verification

- Unit-style manual checks in browser console or a small dev page after routes exist:
  - generic CTA
  - `/pricing?path=exam`
  - `/pricing?path=kids`
  - `/pricing?path=adult`
  - repeated CTA clicks
- Confirm no component imports an analytics vendor directly.

## Out Of Scope

- Analytics vendor installation.
- Final prepared Telegram message approval.
- Public launch decision for message text.

## Tasks/Subtasks

- [x] Add Telegram CTA and tracking contracts.
  - [x] Add dependency-free verification for Telegram CTA and tracking contracts.
  - [x] Add shared CTA context and tracking payload types.
  - [x] Add `useTelegramCta` with generic fallback, known path/format context normalization, one-place URL encoding, and tracking context output.
  - [x] Add `useTracking` with the seven-event allow-list and safe payload sanitization.
  - [x] Integrate the CTA helper and tracking click contract into header, mobile, and footer Telegram surfaces.
  - [x] Run story verification and `npm run build`.

### Review Findings

- [x] [Review][Patch] Tracking event allow-list is not enforced at runtime [app/composables/useTracking.ts:45]
- [x] [Review][Patch] Telegram CTA resolver trusts runtime locale and message intent values [app/composables/useTelegramCta.ts:65]
- [x] [Review][Patch] Surface-owned Telegram source attribution can be overridden by caller context [app/components/navigation/AppHeader.vue:28]
- [x] [Review][Patch] Direct MobileNav usage can generate default-locale Telegram context [app/components/navigation/MobileNav.vue:28]
- [x] [Review][Patch] Telegram CTA verifier does not execute resolver or tracking behavior [scripts/verify-telegram-cta-and-tracking.mjs:101]

## Dev Agent Record

### Implementation Plan

- Add a dependency-free verifier first so the missing contracts fail before implementation.
- Reuse the existing typed content layer for `Locale`, route ids, and message intents, extending it only where the CTA/tracking contract needs shared public types.
- Keep Telegram URL construction pure and centralized, then let navigation components consume the computed URL and click tracker.
- Keep tracking vendor-free with a typed no-op/dev adapter until analytics selection is in scope.

### Debug Log

- 2026-07-12: Started story implementation and added the Telegram CTA/tracking verifier in red phase.
- 2026-07-12: Red-phase `npm run test:telegram-cta` failed on missing `TelegramPathContext`, confirming the verifier caught the absent contract before implementation.
- 2026-07-12: Adjusted verifier declaration-shape checks so they validate exported const composables and normalizer fallback expressions without requiring a specific function-declaration style.
- 2026-07-12: Existing app-shell verifier expected the old raw `telegram-href` prop; updated it to assert the new shared `telegram-context` contract.
- 2026-07-12: Final validation passed with `npm run test:telegram-cta`, `npm run test:content-locale`, `npm run test:app-shell`, `npm run test:ui-foundation`, and `npm run build`.
- 2026-07-12: Runtime localhost check confirmed the rendered Nuxt page exposes two Telegram CTA hrefs using `https://t.me/fluyo_manager?text=...`.
- 2026-07-13: Code review patches added runtime event allow-list enforcement, runtime CTA context normalization, source attribution ownership, direct MobileNav locale handling, and behavior-level verifier assertions; final validation passed.

### Completion Notes

- Added shared CTA/tracking types for Telegram path context, format context, CTA source route, optional CTA context, and sanitized tracking payloads.
- Added `useTelegramCta` with the production base URL `https://t.me/fluyo_manager`, centralized `encodeURIComponent` handling, generic fallbacks, distinguishable exam/kids/adult contexts, and stable click/context payloads.
- Added `useTracking` with the seven allowed DD-001 event names, typed `trackEvent`, undefined-safe payload sanitization, and a dev console adapter until a vendor is selected.
- Replaced the hard-coded Telegram navigation URL with helper-generated URLs in header, mobile nav, and footer surfaces.
- Wired Telegram icon buttons to emit `telegram_context` and `telegram_click` through the shared helper on click.
- Added `npm run test:telegram-cta` and kept existing app-shell verification aligned with the new CTA context contract.
- Verified rendered header/footer Telegram hrefs through the running local Nuxt server.
- Resolved code review findings by enforcing allowed tracking events at runtime, normalizing invalid CTA locale/message intent values, preserving surface-owned source attribution, and adding behavior-level verifier execution for CTA/tracking outputs.
- No analytics vendor was added.

## File List

- `_bmad-output/implementation-artifacts/DD-001-fluyo-production-site/stories/DD-001.1.2-telegram-cta-and-tracking-contracts.md`
- `app/components/navigation/AppHeader.vue`
- `app/components/navigation/FooterContactStrip.vue`
- `app/components/navigation/MobileNav.vue`
- `app/components/navigation/navigationLinks.ts`
- `app/composables/useTelegramCta.ts`
- `app/composables/useTracking.ts`
- `app/data/content/types.ts`
- `package.json`
- `scripts/verify-ui-primitives-and-app-shell.mjs`
- `scripts/verify-telegram-cta-and-tracking.mjs`

## Change Log

- 2026-07-12: Started story implementation, added baseline story bookkeeping, and added red-phase verification script.
- 2026-07-12: Added shared Telegram CTA/tracking types and composables, integrated the helper into navigation CTA surfaces, updated verification, and set status to review.
- 2026-07-13: Applied code review patches and set status to done.
